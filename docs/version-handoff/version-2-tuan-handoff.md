# P-056 RELEASE V2 — TUAN HANDOFF## 1. Handoff metadata| Field | Value ||---|---|| Project | P-056 Release V2 || Branch | `feature/TQT-reference-ragas` || Owner scope | Tuấn — Data and AI Quality || Baseline commit | `8fb263ea3043476a92aecc41b9c40e4ea7828239` || Final commit before handoff | `fd9aaec79aaada2487bfae997f26c76ea7860bf8` || Handoff date | 2026-08-05 || Delivery status | READY FOR TIP-006 VERIFY |---## 2. Scope and ownership### Tuấn owns- Reference data filtering and quarantine- Normal reference lookup- Data and checker tests- RAGAS baseline evaluation### Tuấn did NOT change ownership of- `src/agents/graph.py` — graph architecture- `src/agents/state.py` — shared state contract- Analyzer and RAG architecture- Guardrail policy- Public API production schema- Critical threshold policy- Authentication and deployment configurationNo modifications to any of the above were made in this branch.---## 3. Delivery summary| Item | Count ||---|---|| Source reference rows | 80 || Strict-quality eligible | 58 || Runtime accepted | 58 || Quarantined | 22 || Multiple-reason rows | 2 || Approved analytes (normal RI) | 4 || Pending analytes | 5 || RAGAS dataset cases | 12 || RAGAS metric-eligible cases | 11 || RAGAS not-applicable cases | 1 || Post-live test count | 209 passed |---## 4. Commit map| TIP | Description | Commit hash ||---|---|---|| TIP-000 | Focused scan | `e90f9563bff354e667ea5dbba928fb13604c4657` || TIP-001 | V2 whitelist/quarantine build | `1170e32369153e7ab8b60eabf3ab43731df35b97` || TIP-002A | Characterization tests | `54eeec4b467d8318488b443243fb794ffb9ba656` || TIP-002B | Checker with V2 config | `6b012a4f5a04eff176555dbc157c50908d10c9de` || TIP-003 | Integration verification | `d8076a4e3ff93ff70b8144b9f5d0b449f209502c` || TIP-004A | RAGAS scaffold | `01205af4aade3440b04f008762369d6d69873a77` || TIP-004B | Live RAGAS baseline | `fd9aaec79aaada2487bfae997f26c76ea7860bf8` || TIP-005 | Handoff package | pending — see final commit |All hashes above are verified resolvable in Git as `commit` objects.---## 5. Reference-data build- **Source file:** `adult_outpatient_laboratory_reference_map.csv`- **Source SHA-256:** `721A07EA6EC8BC202DAD4F23808ECA34853A146E41E3EC3AE04EC66EABDA7D6B`- Input rows: **80**- Strict-quality eligible: **58**- Runtime accepted: **58**- Quarantined: **22**- Multiple-reason rows: **2**Rejection reason counts:| Reason | Count ||---|---:|| `range_flag_not_ok` | 22 || `confidence_not_high` | 2 || `unsupported_source_tier` | 2 |---## 6. Runtime and quarantine artifacts| Artifact | Path ||---|---|| Accepted ranges (CSV) | `data/reference/reference_ranges_v2.csv` || Accepted ranges (JSON) | `data/reference/reference_ranges_v2.json` || Quarantine log | `data/reference/quarantine_v2.csv` || Build report | `data/reference/reference_build_report.json` || Checker config | `data/reference/reference_checker_v2_config.json` || Analyte manifest | `docs/version-handoff/v2_analyte_manifest.json` |---## 7. Nine-analyte status| Analyte | Normal RI status | Unit status | Age status | Critical rule | RAGAS case | Blocker ||---|---|---|---|---|---|---|| WBC | approved | validated | validated | none | yes | — || RBC | approved | validated | validated | none | yes | — || HGB | **pending** | conflict | validated | none | no | `unit_data_conflict` || Fasting plasma glucose | approved | validated | validated | yes | yes | — || HbA1c | **pending** | not validated | validated | none | no | `md_not_approved` || LDL-C | **pending** | not validated | validated | yes | no | `md_not_approved` || HDL-C | **pending** | conflict | validated | none | no | `cdl_only, unit_policy_conflict` || Creatinine | approved | validated | validated | none | yes | — || Potassium | **pending** | not validated | validated | yes (Critical Detector) | no | `normal_reference_not_approved, critical_detector_owned` |**Four analytes are runtime-approved for normal RI lookup: WBC, RBC, Fasting plasma glucose, Creatinine.****Five analytes remain pending: HGB, HbA1c, LDL-C, HDL-C, Potassium.**---## 8. Reference Repository and CheckerThe V2 checker uses `data/reference/reference_checker_v2_config.json` to load approved rules at runtime. Only analytes with `approval_status = "approved"` and `normal_reference_type = "RI"` are dispatched for normal-range classification.Pending analytes are not dispatched. CDL and MD rows remain in the quarantine or are silently excluded from the normal lookup path.---## 9. Unit policy### Approved textual aliases (no numeric conversion)```10^3/uL  ->  10^9/L10^3/µL  ->  10^9/L10^3/μL  ->  10^9/L```The numeric value is unchanged because these representations are equivalent for count-per-volume.### Forbidden equalities```mg/dL  !=  mmol/Lg/L    !=  10^9/L```HGB remains pending because its runtime data uses `10^9/L` while `units_metric.csv` records `g/L`. These units are not equivalent and must not be silently aliased.---## 10. Age and sex policy- Project-local `Adult` mapping: **18–60 inclusive**.- Exact sex rule has priority over ambiguous fallback.- Safe `A` (all-sex) fallback is allowed only when the rule is unambiguous.- Unknown or invalid gender must fail safely rather than silently selecting an incorrect rule.---## 11. Critical Detector separationCritical threshold classification is a **separate policy** owned by the Critical Detector module. The normal reference checker does not own or evaluate critical thresholds.Potassium (`Kali`) is explicitly pending for normal-range lookup. The Critical Detector owns Potassium critical classification independently.---## 12. API and output-contract compatibilityNo changes were made to the public API production schema. The checker output format is compatible with the existing API contract. The graph, state, and shared service contracts are unchanged.---## 13. Automated-test evidence| Suite | Result ||---|---|| Post-live full safe suite | 209 passed, 0 failed, 0 xfailed, 0 xpassed || Compile (`compileall src eval tests`) | exit code 0 || `pip check` | No broken requirements found || Warnings | 2 (see limitations) |The full suite ran after the live RAGAS baseline was completed and all authorized TIP-004B files were staged, as required by Phase 0.4 of TIP-005.---## 14. RAGAS dataset and runner| Field | Value ||---|---|| Dataset path | `eval/datasets/ragas_v2_baseline.jsonl` || Dataset version | `v2-baseline-1` || Dataset SHA-256 | `3f1cb84cebde5a5a47fca5eafca0a0276ebe6c04665fd9bb0531fe92ea24da2f` || Total cases | 12 || Metric-eligible cases | 11 || Not-applicable cases | 1 || Approved analytes only | WBC, RBC, Fasting plasma glucose, Creatinine |**These contexts are curated evaluation fixtures, not contexts captured from the production graph.**The runner is `eval/run_ragas.py`. A project-local compatibility shim (`eval/ragas_compat.py`) resolves a `ragas==0.4.3` Vertex AI import problem in this environment. The shim patches nothing in `.venv/site-packages`.---## 15. RAGAS live baseline| Metric | Count | Mean | Median | Min | Max | Std dev | N/A ||---|---:|---:|---:|---:|---:|---:|---:|| Faithfulness | 11 | 0.909091 | 1.000000 | 0.500000 | 1.000000 | 0.192847 | 1 || Context Precision | 11 | 0.954545 | 1.000000 | 0.500000 | 1.000000 | 0.143740 | 1 || Custom hallucination proxy | 11 | 0.090909 | 0.000000 | 0.000000 | 0.500000 | 0.192847 | 1 |Additional facts:- Total dataset cases: 12- Metric-eligible: 11- `RAGAS-V2-012`: `not_applicable` because retrieved_contexts is empty- Failed full-baseline cases: 0- Unsupported-claim contrast check: passed- Relevant-first context-order contrast check: passed- Dataset or prompt tuning after seeing scores: NO- Full baseline invocation count: exactly 1> **Interpretation boundary:** This is a curated-fixture baseline for evaluation plumbing and metric visibility. It evaluates curated fixtures, not retrieved contexts captured from the production graph. It is not production RAG validation, not clinical validation, and not evidence that all analytes are approved.Evaluator configuration:| Field | Value ||---|---|| Provider | google || Model | gemini-2.5-flash || Transport adapter | google_openai_compatible || Transport library | openai-python || Endpoint host | generativelanguage.googleapis.com || RAGAS version | 0.4.3 || Credential variable | GOOGLE_API_KEY || OpenAI service used | NO || OPENAI_API_KEY used | NO || Concurrency | 1 || Automatic retries | 0 |---## 16. Security and credential handling- API keys were accidentally visible in an earlier screenshot. The human confirmed that exposed keys were revoked and replaced.- `.env` is git-ignored and untracked. It does not appear in `git status`.- No key values were ever committed to the repository.- The live evaluation used `GOOGLE_API_KEY` loaded from the local environment.- OpenAI service was not used. `OPENAI_API_KEY` was not used.- The transport used the OpenAI-compatible protocol targeting the Google endpoint.- Credential scan of all candidate TIP-004B artifacts returned zero matches.---## 17. Reproduction commands### Build reference data```powershellF:\VIN_AI_PROJECT\.venv\Scripts\python.exe -B -m src.data.build_reference_v2```### Focused checker tests```powershellF:\VIN_AI_PROJECT\.venv\Scripts\python.exe -B -m pytest `  tests/test_services `  tests/test_data `  -q -rxX -p no:cacheprovider```### Full safe tests```powershellF:\VIN_AI_PROJECT\.venv\Scripts\python.exe -B -m pytest `  tests `  -q -rxX -p no:cacheprovider `  --ignore=tests/test_embed.py```### RAGAS validate-only (offline, no live API)```powershellF:\VIN_AI_PROJECT\.venv\Scripts\python.exe -B -m eval.run_ragas `  --dataset eval/datasets/ragas_v2_baseline.jsonl `  --validate-only```### RAGAS live baseline (requires GOOGLE_API_KEY in environment, no key embedded)```powershellF:\VIN_AI_PROJECT\.venv\Scripts\python.exe -B -m eval.run_ragas `  --dataset eval/datasets/ragas_v2_baseline.jsonl `  --live `  --provider google `  --model gemini-2.5-flash `  --max-cases 12 `  --confirm-key-rotated `  --skip-probe-after-confirmed `  --output eval/results/ragas_v2_baseline.json```---## 18. Protected-file integrity| File or path | Status ||---|---|| `src/` | unchanged || `data/reference/` | unchanged || `adult_outpatient_laboratory_reference_map.csv` | unchanged || `requirements.txt` | unchanged || `.env` | ignored and untracked || `.env.example` | unchanged || `.gitignore` | unchanged || `docs/version-handoff/v2_analyte_manifest.json` | unchanged |---## 19. Known limitations1. **HGB** remains pending due to `unit_data_conflict`: runtime RI rows use `10^9/L` while `units_metric.csv` records `g/L`.2. **HDL-C** remains pending: available runtime rules are CDL-only and there is a `unit_policy_conflict`.3. **HbA1c** remains pending: MD status — not approved as normal RI.4. **LDL-C** remains pending: MD status — not approved as normal RI.5. **Potassium** normal RI remains pending: Critical Detector owns critical classification; normal checker is not approved.6. **RAGAS contexts are curated fixtures.** The production graph does not currently expose retrieved contexts for direct RAGAS evaluation.7. Direct unbootstrapped RAGAS import has a Vertex AI compatibility issue. The project-local shim `eval/ragas_compat.py` is the approved workaround.8. A `langchain-community` deprecation warning is present in the test suite.9. RAGAS baseline scores may vary across repeated evaluator runs.---## 20. Pending decisionsThe following require owner decisions before they can be resolved:| Item | Owner | Decision needed ||---|---|---|| HGB source-unit conflict | Data/medical owner | Confirm correct unit for HGB runtime rows || HDL-C CDL-only policy | Data/medical owner | Determine if CDL rules can be promoted to RI or if new RI source is needed || HbA1c MD status | Medical owner | Approve or reject as normal RI || LDL-C MD status | Medical owner | Approve or reject as normal RI || Potassium normal-reference approval | Medical/data owner | Approve normal RI or confirm Critical Detector sole ownership || Retrieved-context capture for RAGAS | Architecture owner (Vũ) | Determine approach to expose production retrieved contexts for future RAGAS evaluation |These decisions are not resolved in TIP-005.---## 21. Rollback procedureThe following `git revert` commands safely reverse specific TIPs without destructive operations. Provide these commands to the reviewer but do not execute them without explicit instruction.```bash# Reverse TIP-004B (live baseline)git revert fd9aaec79aaada2487bfae997f26c76ea7860bf8# Reverse TIP-004A (scaffold)git revert 01205af4aade3440b04f008762369d6d69873a77# Reverse TIP-003 (integration)git revert d8076a4e3ff93ff70b8144b9f5d0b449f209502c```**Do not use `git reset --hard` or `git push --force`.** Safe rollback uses `git revert` to create a new commit that reverses the target, preserving history.---## 22. Reviewer checklist### For Vũ (architecture review)- [ ] No changes to `src/agents/graph.py`- [ ] No changes to `src/agents/state.py`- [ ] No changes to analyzer or RAG architecture- [ ] Checker output contract is backward-compatible- [ ] RAGAS uses curated contexts — not production graph retrieved contexts- [ ] Agree on approach for future runtime retrieved-context capture for RAGAS### For data/medical owner- [ ] HGB source-unit conflict (runtime `10^9/L` vs unit map `g/L`) — decision required- [ ] HDL-C CDL-only policy — decision required- [ ] HbA1c MD status — approve or reject as normal RI- [ ] LDL-C MD status — approve or reject as normal RI- [ ] Potassium normal-reference approval — decision required---## 23. Readiness recommendation```READY FOR TIP-006 VERIFY```This branch is ready for independent verification (TIP-006). It is NOT recommended for production deployment until TIP-006 VERIFY passes and the pending analyte decisions are resolved.Do not mark this as `SHIP`, `PRODUCTION READY`, or `CLINICALLY VALIDATED`.---## Deviations### DEV-004B-01The one-case metric smoke command was executed twice. Attempt 1 failed with HTTP 429 RESOURCE_EXHAUSTED because prepayment credits were depleted. Attempt 2 succeeded. The full 12-case baseline was executed exactly once. No score tuning or credential leak occurred.Contractor disposition: **waived_and_documented**.### DEV-TIP004B-02The authorized TIP-004B files were committed together with guide documentation files (`docs/guide/`) in a single commit (`fd9aaec79aaada2487bfae997f26c76ea7860bf8`) rather than as a separate commit containing only the authorized TIP-004B files. The commit message also differs from the specified `feat(eval): record Gemini RAGAS V2 baseline`. Content validity of the result artifacts is not affected.Contractor disposition: **documented**.