# BÀN THI CÔNG — TIP-007

## VMEC-05 USER-FACING AI ORCHESTRATOR V1

```text
TIP=TIP-007
NAME=VERIFY + DOCUMENTATION
TYPE=FINAL VERIFICATION / DOCUMENTATION / HANDOVER EVIDENCE
BUILDER=Tạ Quốc Tuấn / Codex
HUMAN OWNER=Nguyễn Tuấn Dương
```

---

# 1. CURRENT PROJECT STATE

Approved project state entering this TIP:

```text
TIP-001=APPROVED
TIP-002=APPROVED
TIP-003=APPROVED
TIP-004=APPROVED
TIP-005=APPROVED
TIP-006=APPROVED

ESC-003=CLOSED
ESC-004=CLOSED

TIP-007=CURRENT
```

Latest accepted TIP-006 evidence:

```text
P0 authorization violations=0
P0 cross-user leakage=0
P0 medical authority violations=0
P0 OCR bypasses=0
P0 invalid privileged action execution=0

Intent Routing=20/20=100%
Workflow Selection=6/6=100%
Suggested Action schema validity=7/7=100%
Context Resolution=10/10=100%
Task Completion=6/6=100%

E2E=6/6 PASS

Backend full regression=941 passed
Orchestrator=113 passed
Frontend tests=25 passed
Frontend lint=PASS
Frontend TypeScript=PASS
Frontend build=PASS
```

Do not merely copy these numbers into the Final Verify Report.

TIP-007 must independently verify current repository state.

---

# 2. PRIMARY GOAL

Final acceptance against:

```text
RRI requirements
Blueprint
Contract
Human Owner decisions
TIP-001..TIP-006 accepted behavior
current repository implementation
```

TIP-007 answers:

```text
What was promised?
What was implemented?
What was tested?
What is missing?
What is deferred?
What remains risky?
Are docs accurate?
Is there evidence supporting deployment readiness?
```

---

# 3. THIS IS NOT A FEATURE TIP

Do NOT add:

```text
new intent
new SuggestedAction
new user role
new chatbot feature
new medical feature
new memory
new RAG behavior
new API capability
new database model
new UI redesign
```

TIP-007 is:

```text
VERIFY
DOCUMENT
TRACE
REPORT
```

Only local documentation corrections and verification-oriented test/evidence additions are expected.

If verification discovers an implementation defect:

```text
DO NOT silently expand TIP-007 into a feature/refactor TIP.
```

Follow the defect rules below.

---

# 4. READ SOURCE OF TRUTH FIRST

Read repository's current:

```text
presentation/AGENT.md
```

Then locate and read the actual project artifacts where present:

```text
RRI Report / Requirements Matrix
Blueprint
Contract
Solo TIP Pack
Master Handover
TIP Completion Reports
Escalation/Debug Reports
TIP-006 evaluation evidence
README
.env.example
API docs
architecture docs
evaluation docs
```

Do not reconstruct a missing artifact and then verify your reconstruction.

If an expected artifact is not present:

```text
CANNOT DETERMINE
```

and state exactly what is missing.

---

# 5. PRE-VERIFY INVENTORY

Before editing any file, produce internally:

```text
TIP007_PRE_VERIFY_SCAN

RRI_ARTIFACT=
BLUEPRINT_ARTIFACT=
CONTRACT_ARTIFACT=

TIP001_REPORT=
TIP002_REPORT=
TIP003_REPORT=
TIP004_REPORT=
TIP005_REPORT=
TIP006_REPORT=

CURRENT_README=
CURRENT_ENV_DOC=
CURRENT_API_DOC=
CURRENT_ARCH_DOC=
CURRENT_EVAL_DOC=

CURRENT_BACKEND_TEST_BASELINE=
CURRENT_FRONTEND_TEST_BASELINE=

DOCUMENTATION_GAPS=
TRACEABILITY_GAPS=
VERIFY_BLOCKERS=
```

If a P0/safety/architecture blocker is discovered:

```text
STOP AND ESCALATE
```

---

# 6. REQUIREMENT TRACEABILITY — CORE OF TIP-007

Build a traceability matrix from the actual project requirements.

For every real REQ-ID found in the project RRI/Contract:

```text
REQ_ID=
REQUIREMENT=
PRIORITY=
SOURCE=
IMPLEMENTATION=
TIP=
TEST_EVIDENCE=
CURRENT_RESULT=
STATUS=
```

Allowed STATUS:

```text
IMPLEMENTED
MISSING
DEFERRED
PARTIAL
CANNOT_DETERMINE
```

Map by **content**, never by numerical/list position.

Forbidden:

```text
REQ-001 → first test
REQ-002 → second test
```

unless content independently proves that mapping.

---

# 7. IF ACTUAL REQ-ID MATRIX IS MISSING

Do NOT invent REQ-IDs.

Report:

```text
RRI_REQUIREMENT_MATRIX_STATUS=NOT FOUND
REQ_ID_TRACEABILITY=CANNOT DETERMINE
```

You may still verify Contract/TIP requirements using:

```text
CONTRACT_REFERENCE=
TIP_REFERENCE=
TEST_EVIDENCE=
```

But do not create fake project history.

---

# 8. REQUIREMENT COVERAGE

Calculate:

```text
REQUIREMENTS_TOTAL=
IMPLEMENTED=
MISSING=
DEFERRED=
PARTIAL=
CANNOT_DETERMINE=
```

Where actual requirements exist:

```text
IMPLEMENTED_COVERAGE =
IMPLEMENTED / REQUIREMENTS_TOTAL * 100
```

Report P0 requirement coverage separately.

Final VERIFY→SHIP gate requires:

```text
P0 requirements implemented and tested = 100%
```

Any missing P0:

```text
READY_FOR_DEPLOYMENT=NO
```

---

# 9. FROZEN CONTRACT VERIFICATION

Execute checks proving current repository still has exactly:

```text
6 intents
7 SuggestedActions
2 conversational V1 roles: GUEST, PATIENT
19 reason codes
```

And:

```text
Doctor conversational support=DEFER_TO_V2
```

Do not verify by comments alone.

Use executable schema/enum/introspection tests where appropriate.

No seventh intent.

No eighth action.

No unrestricted OTHER.

---

# 10. RESPONSE AUTHORITY VERIFICATION

Verify current code still enforces:

```text
LLM may generate:
message only
```

Server controls:

```text
intent
status
reason_code
data
data_type
sources
suggested_actions
safety_notice
```

Verify final chain remains:

```text
Response Composer
→ Medical Safety Validation
→ Schema Validation
→ Suggested Action Policy Validation
→ Frontend
```

No bypass path.

---

# 11. MEDICAL AUTHORITY FINAL VERIFY

Re-prove:

```text
LLM cannot determine LOW/NORMAL/HIGH
LLM cannot determine CRITICAL
LLM cannot select reference range
LLM cannot diagnose
LLM cannot infer cause
LLM cannot recommend treatment
LLM cannot override canonical medical result
LLM cannot bypass OCR review
LLM cannot bypass final safety validation
```

Use existing TIP-006/evaluation tests plus focused current verification.

Do not create new clinical rules.

Target:

```text
MEDICAL_AUTHORITY_VIOLATIONS=0
```

---

# 12. AUTHORIZATION FINAL VERIFY

Re-prove:

```text
patient identity is server-controlled
arbitrary patient_id cannot be supplied by LLM/tool caller
ownership checked before protected data enters LLM
non-owned and not-found use protected indistinguishable policy
cross-user leakage=0
```

Include Patient A → Patient B negative case.

Target:

```text
AUTHORIZATION_VIOLATIONS=0
CROSS_USER_LEAKAGE=0
```

---

# 13. ZERO-INVOCATION FINAL MATRIX

Re-run/reuse executable evidence for:

```text
ONBOARDING_NOT_ACKNOWLEDGED
→ pipeline=0
→ LLM=0
→ DB=0
→ wrappers=0

DOCTOR_TOKEN
→ LLM router=0
→ wrapper=0
→ DB=0

OCR_PENDING / SKIP ATTEMPT
→ medical pipeline=0

GUEST_HISTORY
→ protected DB query=0

GUEST_TREND
→ protected DB query=0

UNSUPPORTED_ANALYTE
→ general RAG=0
→ explanation LLM=0

NON_OWNED_REPORT
→ protected resource bytes in LLM prompt=0
```

Count at actual wrapped/test-double boundaries.

Do not use self-reported implementation flags.

---

# 14. OCR FINAL VERIFY

Verify lifecycle:

```text
upload
→ PENDING
→ human confirm
→ analysis
→ CONSUMED
```

Re-prove:

```text
unreviewed OCR cannot analyze
expired review cannot analyze
cross-user review cannot analyze
replay cannot trigger second analysis
```

And:

```text
/ocr/confirm
```

remains the sole OCR-derived medical entry.

Target:

```text
OCR_BYPASS=0
```

---

# 15. ESC-003 INTEGRITY FINAL VERIFY

Verify:

```text
data/reference/explanations.json
```

current accepted corpus hash remains:

```text
a26e050e91122a4615990c56176aa2eb53e80be9e2250bacf05700a7ac45a0f4
```

and manifest contains the accepted digest.

Run existing integrity regression.

Do NOT edit:

```text
data/reference/*
```

Any mismatch:

```text
STOP
NEW ESCALATION REQUIRED
```

Historical ESC-003 exception is closed.

---

# 16. ESC-004 FINAL VERIFY

For:

```text
status="unknown"
```

prove:

```text
classification remains unknown
critical inference absent
general RAG calls=0
explanation LLM calls=0
```

Approved curated catalog explanation/sources may remain.

Do not modify analyzer_node.

Historical ESC-004 exception is closed.

---

# 17. INTENT ROUTING FINAL BENCHMARK

Run current approved benchmark.

Target:

```text
>=95%
```

Report actual:

```text
CORRECT=
TOTAL=
PERCENT=
```

List every failure.

Do not copy TIP-006 metric without executing or referencing reproducible current test evidence.

---

# 18. WORKFLOW SELECTION FINAL BENCHMARK

Target:

```text
>=95%
```

Report:

```text
CORRECT=
TOTAL=
PERCENT=
```

Routing and workflow errors remain separate metrics.

---

# 19. SUGGESTED ACTION FINAL VERIFY

Exact allowlist:

```text
OPEN_REPORT
VIEW_ABNORMAL
VIEW_HISTORY
VIEW_TREND
VIEW_DOCTOR_QUESTIONS
CONFIRM_OCR
RETRY
```

Target:

```text
schema validity=100%
invalid privileged action execution=0
```

Re-test:

```text
unknown action
arbitrary URL
javascript:
patient_id
user_id
raw internal route
malformed action
```

Frontend must still not parse navigation from prose.

---

# 20. CONTEXT RESOLUTION FINAL BENCHMARK

Target:

```text
>=90%
```

Verify:

```text
current_report_ref
current_analyte
last_intent
transient ui context
ambiguous context → NEEDS_INPUT
```

Explicitly re-test the production changes introduced in TIP-006:

```text
approved-analyte extraction
successful history/trend context persistence
```

These changes must be part of final regression evidence.

---

# 21. TASK COMPLETION FINAL BENCHMARK

Target:

```text
>=90%
```

Re-run/reproduce current supported task-completion benchmark.

Safe rejection of an intentionally unsafe/unauthorized request counts as successful completion only where blocking is the expected outcome.

Report scoring method.

---

# 22. E2E FINAL VERIFY

Verify all six approved flows:

```text
A. Guest manual
B. Guest OCR
C. Patient analysis + persistence
D. Patient history
E. Patient trend follow-up
F. Unsafe request
```

Required:

```text
A=PASS
B=PASS
C=PASS
D=PASS
E=PASS
F=PASS
```

Any P0 flow failure:

```text
READY_FOR_DEPLOYMENT=NO
```

---

# 23. FAILURE BEHAVIOR FINAL VERIFY

Check applicable frozen reasons:

```text
ONBOARDING_REQUIRED
OCR_REVIEW_REQUIRED
OCR_CONFIRM_INVALID
MEDICAL_DIAGNOSIS_REQUEST
MEDICAL_CAUSE_REQUEST
TREATMENT_REQUEST
UNSUPPORTED_ANALYTE
UNSUPPORTED_CAPABILITY
AMBIGUOUS_CONTEXT
AUTH_EXPIRED
REPORT_NOT_FOUND_OR_UNAUTHORIZED
DB_UNAVAILABLE
LLM_UNAVAILABLE
RAG_UNAVAILABLE
TREND_INSUFFICIENT_POINTS
TREND_UNIT_INCONSISTENT
UNKNOWN_INTENT
GUARDRAIL_BLOCKED
INTERNAL_WORKFLOW_ERROR
```

No new reason code.

Verify bounded, typed behavior.

No fabricated medical/history/trend data.

---

# 24. RAG VERIFY

Verify structural guarantees:

```text
sources are server-controlled
LLM cannot invent source URL
unsupported analyte cannot general-RAG
RAG unavailable uses only existing approved fallback
```

TIP-006 did NOT generate a live RAGAS quality score.

TIP-007 must not invent one.

If no existing reliable live evaluator is available:

```text
RAG_LIVE_QUALITY=
NOT MEASURED
```

with reason.

This alone is not a P0/P1/P2 failure unless a project requirement explicitly requires a live score.

---

# 25. FRONTEND FINAL VERIFY

Verify Patient/Guest Hybrid Assistant still provides:

```text
floating AI Assistant launcher
chat panel
mandatory onboarding flow
structured result rendering
suggested actions
OCR confirm navigation
Guest restrictions
Patient History/Trend navigation
```

Verify:

```text
no prose routing
no arbitrary URL/javascript execution
no frontend medical classification logic
```

---

# 26. RESPONSIVE / ACCESSIBILITY VERIFY

Use existing tooling where available.

Check:

```text
375px
768px
1440px
```

At minimum:

```text
launcher reachable
panel usable
input usable
actions usable
OCR confirm reachable
```

Accessibility basics:

```text
real semantic buttons
labels
keyboard open/close
focus behavior
aria-live/message readability
```

Do not add a new browser/E2E dependency solely for this TIP.

---

# 27. PRIVACY VERIFY

Inspect current code/docs/logging.

Never expose unnecessarily:

```text
full name
phone
email
government ID
patient_id
user_id
DB primary keys
auth token
OCR token
ownership metadata
raw PHI
raw lab values
raw chat logs
```

Verify no inappropriate browser console/debug logging remains in Assistant paths.

Do not claim:

```text
HIPAA compliant
GDPR compliant
regulatory certified
```

unless project has explicit external evidence—which current V1 contract does not provide.

---

# 28. BASIC HEALTH

QA Tier 1 requires:

```text
application starts
core flow works
navigation works
no placeholder content
no broken critical links/assets
build succeeds
```

Check relevant repository/app surfaces.

Search for suspicious production placeholders:

```text
TODO
FIXME
Lorem ipsum
placeholder
not implemented
```

Do not delete legitimate test/comments blindly.

Report only actual user-facing/release-impacting findings.

---

# 29. DOCUMENTATION — REQUIRED

Update documentation as applicable:

```text
README
.env.example / env-config documentation
API documentation
Orchestrator architecture documentation
intent documentation
role/capability documentation
reason/error-code documentation
SuggestedAction documentation
OCR lifecycle/HITL documentation
test/evaluation evidence
known limitations
```

No documentation may claim a capability that code does not implement.

---

# 30. README REQUIRED CONTENT

Ensure README or project entry documentation accurately explains:

```text
what VMEC-05 Orchestrator V1 does
supported roles
six intents
Hybrid Assistant
onboarding
OCR HITL requirement
History/Trend authorization
how to run backend
how to run frontend
how to run tests
important environment variables
known V1 limitations
```

Do not duplicate large architecture documents unnecessarily.

Link internally where repository convention supports it.

---

# 31. ENV / CONFIG DOCUMENTATION

Verify actual settings used by code against:

```text
.env.example
README/config docs
```

Document only real settings.

Do not expose secrets.

Do not commit `.env`.

Do not invent unused configuration keys.

---

# 32. API DOCUMENTATION

Document/reconcile current Orchestrator endpoints, including as applicable:

```text
message endpoint
onboarding acknowledgement
OCR upload/confirm integration
authentication expectations
typed response shape
status/reason codes
SuggestedAction behavior
```

Do not document direct internal wrappers as public APIs unless they are actually public.

---

# 33. ARCHITECTURE DOCUMENTATION

Document current implemented flow:

```text
Patient/Guest UI
→ Orchestrator API
→ role/onboarding/policy gates
→ Context Resolver
→ Intent Router
→ Workflow Dispatcher
→ safe existing services
→ Response Composer
→ Medical Safety Validation
→ Schema Validation
→ SuggestedAction Validation
→ Frontend
```

Also document OCR separately:

```text
Image
→ OCR draft
→ human review
→ /ocr/confirm
→ fixed medical workflow
```

Do not describe an architecture that was planned but never implemented.

---

# 34. INTENT / ROLE DOCUMENTATION

Document exactly six intents:

```text
INT-00 UNSUPPORTED_OR_UNSAFE
INT-01 ANALYZE_REPORT
INT-02 EXPLAIN_CURRENT_RESULT
INT-03 VIEW_HISTORY
INT-04 ANALYZE_TREND
INT-05 GET_DOCTOR_QUESTIONS
```

Roles:

```text
GUEST
PATIENT
```

Doctor conversational Assistant:

```text
DEFERRED TO V2
```

Do not imply Doctor chatbot support exists.

---

# 35. ERROR / REASON DOCUMENTATION

Document the actual frozen reason-code set from schema.

Do not manually assume count/content if repository differs from docs.

Verify against:

```text
src/models/orchestrator_schemas.py
```

Documentation must match executable schema.

---

# 36. TEST / EVALUATION EVIDENCE DOCUMENTATION

Document actual latest final results:

```text
P0
P1
P2
E2E
backend regression
frontend tests
lint
TypeScript
build
```

Do not overwrite history with unexecuted numbers.

Include date/environment where repository convention supports it.

Explicitly record:

```text
RAG live quality score not measured
```

if still true.

---

# 37. KNOWN LIMITATIONS

Document real V1 limitations, including where applicable:

```text
Doctor conversational support deferred
no persistent long-term chat memory
RAG optional/fallback behavior
live RAGAS quality score not measured
other accepted/deferred items from RRI/Contract
```

Do not label a defect as an intentional limitation unless evidence supports that classification.

---

# 38. NO SILENT DOC CORRECTION OF CODE

If docs and code disagree:

```text
determine authoritative approved behavior first
```

If code is correct and docs stale:

```text
update docs
```

If docs/Contract are correct and code is wrong:

```text
this is an implementation defect
```

Do not rewrite documentation merely to legitimize incorrect implementation.

---

# 39. DEFECT HANDLING DURING VERIFY

Classify every discovered issue:

```text
P0_CRITICAL
P1_MAJOR
P2_MINOR
DOC_ONLY
ENVIRONMENTAL
CANNOT_DETERMINE
```

### P0 / architecture / safety ambiguity

```text
STOP
TIP_STATUS=BLOCKED
READY_FOR_DEPLOYMENT=NO
```

Issue escalation.

### Local documentation defect

May fix within TIP-007.

### Small verification/test defect

May fix only if:

```text
no frozen contract changes
no architecture changes
no medical policy changes
no auth changes
no forbidden-file change
```

Otherwise escalate.

---

# 40. FORBIDDEN AREAS

All current AGENT.md forbidden files remain forbidden.

Historical exceptions:

```text
D-OCR-2
ESC-003
ESC-004
```

are CLOSED and are NOT reusable permissions.

Do not touch medical/reference/auth/core forbidden areas to make final verification pass.

A failure in a forbidden area is evidence for Human review, not authorization to repair it.

---

# 41. FULL BACKEND REGRESSION

TIP-004's historical waiver does not apply.

TIP-007 final verification requires:

```text
pytest -q
```

unless Human Owner explicitly issues a NEW TIP-007-specific waiver.

Expected final report must contain actual:

```text
PASSED=
FAILED=
ERRORED=
XFAILED=
```

A timeout is:

```text
TIMEOUT
```

not FAIL.

If appropriate, rerun with sufficient execution timeout and report both runs.

---

# 42. FOCUSED BACKEND VERIFY

Run at minimum:

```text
pytest tests/orchestrator -q
```

and focused relevant suites for:

```text
authorization
OCR lifecycle
analyzer/RAG
guardrail
history/trend
medical KB integrity
TIP-006 evaluation
```

Use actual repository paths.

Do not invent commands for files that do not exist.

---

# 43. FRONTEND FINAL HEALTH

From frontend directory, run actual configured commands:

```text
npm test
npm run lint
npx tsc --noEmit
npm run build
```

If any script does not exist:

```text
NOT AVAILABLE
```

Report accurately.

---

# 44. RUFF

Run Ruff on files modified by TIP-007.

If repository-wide Ruff is also run, preserve baseline accounting:

```text
TOTAL=
PRE_EXISTING=
NEW_FROM_TIP007=
```

Required:

```text
NEW_FROM_TIP007=0
```

Do not fix unrelated historical Ruff debt.

Do not run:

```text
make typecheck
mypy
```

Decision D-B remains active.

---

# 45. GIT / WORKTREE REVIEW

Before final report:

```text
git status --short
git diff --stat
git diff --check
```

Separate:

```text
TIP-007 changes
pre-existing uncommitted changes
```

Do not claim unrelated worktree files as TIP-007 work.

Do not automatically clean/revert pre-existing changes.

---

# 46. DOCUMENTATION DIFF REVIEW

For every documentation file modified:

Verify:

```text
statement matches current code
commands are executable
paths exist
intent/action counts correct
roles correct
no false regulatory claim
no stale ESC-003/004 open status
current TIP sequence/status correct
```

---

# 47. AGENT.md FINAL STATUS UPDATE

If project convention permits AGENT.md status maintenance and no instruction prohibits it, update status only after final verification evidence exists.

Expected state after successful TIP-007 verification:

```text
TIP-001 ACCEPTED
TIP-002 ACCEPTED
TIP-003 ACCEPTED
TIP-004 ACCEPTED
TIP-005 ACCEPTED
TIP-006 ACCEPTED
TIP-007 VERIFIED / awaiting Human final gate
```

Do NOT write:

```text
READY FOR DEPLOYMENT
SHIP
PRODUCTION READY
```

unless Human Owner has already explicitly decided it.

Only Human Owner makes that declaration.

---

# 48. ACCEPTANCE CRITERIA

## AC1 — Requirement Traceability

Every available real project requirement is mapped to:

```text
implementation
TIP
test/evidence
current status
```

No fabricated REQ-ID.

---

## AC2 — P0

```text
Authorization violations=0
Cross-user leakage=0
Medical authority violations=0
OCR protected-gate bypass=0
Invalid privileged action execution=0
```

---

## AC3 — P1

```text
Intent Routing >=95%
Workflow Selection >=95%
SuggestedAction schema validity=100%
```

---

## AC4 — P2

```text
Context Resolution >=90%
Task Completion >=90%
```

---

## AC5 — E2E

All six required V1 E2E flows pass.

---

## AC6 — Regression

Full backend regression passes.

---

## AC7 — Frontend

```text
tests=PASS
lint=PASS
TypeScript=PASS
build=PASS
```

---

## AC8 — Medical/Auth/OCR Contracts

Final verification confirms no regression in:

```text
medical authority
ownership
OCR HITL
unsupported analyte policy
zero-invocation gates
```

---

## AC9 — Documentation Accuracy

README/config/API/architecture/evaluation docs accurately represent current implementation.

No unsupported capability claims.

---

## AC10 — Critical Issues

```text
OPEN_P0_CRITICAL_ISSUES=0
OPEN_SAFETY_AMBIGUITIES=0
OPEN_ARCHITECTURE_DEVIATIONS=0
```

---

# 49. FINAL VERIFY REPORT — REQUIRED

Return a dedicated:

# FINAL VERIFY REPORT — VMEC-05 ORCHESTRATOR V1

## VERIFY STATUS

```text
PASS / FAIL / BLOCKED
```

## REQUIREMENT COVERAGE

```text
REQUIREMENTS_TOTAL=
IMPLEMENTED=
MISSING=
DEFERRED=
PARTIAL=
CANNOT_DETERMINE=

P0_REQUIREMENTS_TOTAL=
P0_IMPLEMENTED_AND_TESTED=
P0_COVERAGE=
```

## TRACEABILITY

For each available requirement:

```text
REQ_ID=
REQUIREMENT=
PRIORITY=
IMPLEMENTATION=
TIP=
TEST_EVIDENCE=
STATUS=
```

If no actual REQ matrix exists, clearly state that limitation.

## P0

```text
AUTHORIZATION_VIOLATIONS=
CROSS_USER_LEAKAGE=
MEDICAL_AUTHORITY_VIOLATIONS=
OCR_BYPASSES=
INVALID_PRIVILEGED_ACTION_EXECUTIONS=

P0_STATUS=
```

## P1

```text
INTENT_ROUTING=<correct>/<total>=<percent>
WORKFLOW_SELECTION=<correct>/<total>=<percent>
ACTION_SCHEMA_VALIDITY=<valid>/<total>=<percent>

P1_STATUS=
```

## P2

```text
CONTEXT_RESOLUTION=<correct>/<total>=<percent>
TASK_COMPLETION=<correct>/<total>=<percent>

P2_STATUS=
```

## ZERO-INVOCATION

```text
ONBOARDING=
DOCTOR=
OCR_PENDING=
GUEST_HISTORY=
GUEST_TREND=
UNSUPPORTED_ANALYTE_RAG=
UNSUPPORTED_ANALYTE_LLM=
NON_OWNED_REPORT_PROMPT_BYTES=
```

Include measured counts.

## E2E

```text
GUEST_MANUAL=
GUEST_OCR=
PATIENT_ANALYSIS_PERSISTENCE=
PATIENT_HISTORY=
PATIENT_TREND=
UNSAFE_REQUEST=

E2E_STATUS=
```

## RAG

```text
SUPPORTED_RAG_POLICY=
UNSUPPORTED_ANALYTE_POLICY=
SOURCE_INTEGRITY=
RAG_FAILURE_BEHAVIOR=

LIVE_RAG_QUALITY=
```

Do not fabricate metric values.

## BACKEND

```text
ORCHESTRATOR_TESTS=
FOCUSED_SAFETY_TESTS=
FULL_PYTEST=
RUFF=
```

## FRONTEND

```text
TESTS=
LINT=
TYPESCRIPT=
BUILD=
RESPONSIVE=
ACCESSIBILITY_BASIC=
```

## DOCUMENTATION

```text
README=
ENV_CONFIG=
API_DOC=
ARCHITECTURE_DOC=
INTENT_ROLE_DOC=
ERROR_CODE_DOC=
EVALUATION_EVIDENCE=
KNOWN_LIMITATIONS=
```

## FILES CHANGED

For every TIP-007 file:

```text
PATH=
PURPOSE=
CHANGE=
```

## MISSING / DEFERRED

List every item.

If none:

```text
NONE
```

Do not hide deferred V2 capabilities.

## CRITICAL ISSUES

```text
COUNT=
```

For each:

```text
SEVERITY=
DESCRIPTION=
EVIDENCE=
IMPACT=
RECOMMENDATION=
```

## DEVIATIONS

List all deviations.

## TEST COMMANDS

For every executed command:

```text
COMMAND=
RESULT=
PASSED=
FAILED=
ERRORED=
TIMEOUT=
```

## GIT DIFF SUMMARY

```text
FILES_CHANGED=
NEW_FILES=
INSERTIONS=
DELETIONS=
PRE_EXISTING_WORKTREE_ITEMS=
```

## FINAL DEFINITION OF DONE

Report each:

```text
6_INTENTS_IMPLEMENTED=
GUEST_FLOWS=
PATIENT_FLOWS=
OCR_HITL_NON_BYPASSABLE=
HISTORY_AUTH_SECURE=
TREND_INTEGRATION=
HYBRID_ASSISTANT=
ONBOARDING_ENFORCED=
RESPONSE_SCHEMA_TYPED=
SUGGESTED_ACTIONS_SAFE=
P0_PASS=
P1_PASS=
P2_PASS=
E2E_PASS=
REGRESSION_PASS=
README_DOCS_UPDATED=
VERIFY_REPORT_COMPLETED=
```

## FINAL GATE

Return:

```text
VERIFY_STATUS=

P0_STATUS=
P1_STATUS=
P2_STATUS=
E2E_STATUS=
REGRESSION_STATUS=
DOCUMENTATION_STATUS=

CRITICAL_ISSUES=
OPEN_P0=
OPEN_ARCHITECTURE_DEVIATIONS=
OPEN_SAFETY_AMBIGUITIES=

READY_FOR_DEPLOYMENT_RECOMMENDATION=
YES / NO

FINAL_HUMAN_DECISION_REQUIRED=
YES
```

Important:

```text
READY_FOR_DEPLOYMENT_RECOMMENDATION
```

is Builder verification evidence/recommendation only.

Builder does NOT declare deployment approval.

---

# 50. HUMAN FINAL GATE

After Final Verify Report, STOP.

Do not deploy.

Do not merge/release automatically.

Do not begin V2.

Human Owner Nguyễn Tuấn Dương alone decides:

```text
SHIP
FIX
DEFER
```

If Human selects SHIP:

```text
READY_FOR_DEPLOYMENT=YES
```

may then be recorded by Human decision.

If FIX:

```text
identify exact failed requirement/issues
create scoped corrective TIP/debug task
re-VERIFY affected + regression gates
```

If DEFER:

```text
record exactly what is deferred
priority
reason
risk
follow-up scope
```

---

# 51. STOP CONDITIONS

Immediately stop and escalate if final Verify discovers:

```text
any P0 violation
cross-user leakage
OCR bypass
medical authority violation
invalid privileged action execution
architecture deviation
safety ambiguity
frozen contract mismatch
medical/reference integrity mismatch
need to modify forbidden file
```

Do not repair these silently in TIP-007.

---

# 52. EXECUTION ORDER

Execute exactly:

```text
1. Read current presentation/AGENT.md
2. Locate real RRI / Blueprint / Contract / TIP reports
3. PRE-VERIFY inventory
4. Build requirement traceability by content
5. Verify frozen contracts
6. Run P0 / zero-invocation final checks
7. Run P1/P2 benchmarks
8. Run E2E A-F
9. Verify auth / OCR / medical / RAG / failure behavior
10. Verify frontend integration
11. Run focused backend suites
12. Run frontend tests/lint/types/build
13. Run full pytest -q
14. Verify medical KB integrity
15. Review privacy/logging/basic health
16. Update README/config/API/architecture/eval docs
17. Re-check documentation against executable code
18. Run lint/diff checks
19. Calculate final coverage and metrics
20. Produce FINAL VERIFY REPORT
21. STOP for Human decision
```

Do NOT deploy.

Do NOT start new development.

Do NOT declare SHIP yourself.
