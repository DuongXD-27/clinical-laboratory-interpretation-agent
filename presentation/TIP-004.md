# BÀN THI CÔNG — TIP-004

## PROJECT

VMEC-05 USER-FACING AI ORCHESTRATOR V1

## TIP

```text
TIP-004 — RAG / RESPONSE / FINAL OUTPUT
```

## ROLE

Bạn là **Builder / Codex**.

Design đã được approved.

Không redesign architecture.

Không mở rộng scope.

Không bắt đầu TIP-005.

---

# 1. PROJECT STATE

Current approved state:

```text
TIP-001 = APPROVED
TIP-002 = APPROVED
TIP-003 = COMPLETED / APPROVED

ESC-003 = CLOSED
ESC-004 = CLOSED

OPEN_P0_OR_SAFETY_AMBIGUITY = NO

TIP-004 = CURRENT
TIP-005 = NOT STARTED
TIP-006 = NOT STARTED
TIP-007 = NOT STARTED
```

Pre-TIP-004 decisions already resolved:

```text
ESC-003:
medical_kb_manifest.json corrected to accepted corpus hash
a26e050e91122a4615990c56176aa2eb53e80be9e2250bacf05700a7ac45a0f4

ESC-004:
Option B accepted.

status="unknown":
- no general RAG
- no LLM explanation
- approved curated catalog explanation may remain
- approved catalog sources may remain
```

Do not revisit these decisions.

---

# 2. TIP-004 GOAL

Connect canonical medical/application outputs to the final user-facing Orchestrator response safely.

Implement/wire:

```text
Canonical workflow result
        ↓
Response Composer
        ↓
Medical Safety Validation
        ↓
Response Schema Validation
        ↓
Suggested Action Validation
        ↓
Final typed OrchestratorResponse
```

Primary rule:

```text
LLM Response Composer may generate MESSAGE ONLY.
```

The server owns everything else.

---

# 3. RESPONSE AUTHORITY BOUNDARY

## LLM MAY GENERATE

Exactly:

```text
message
```

## LLM MUST NOT GENERATE OR OVERRIDE

```text
intent
status
reason_code
data
data_type
sources
suggested_actions
safety_notice

LOW / NORMAL / HIGH
CRITICAL
reference range
trend result
question list
report ownership
authorization result
OCR state
action payload
```

If any current implementation allows model output to author these fields:

```text
STOP OR FIX WITHIN TIP-004 ONLY IF
the fix can be made without changing frozen contracts/medical logic.
```

If contract/architecture modification is required:

```text
ESCALATE
```

---

# 4. ABSOLUTE MEDICAL SAFETY RULES

Never let an LLM:

```text
determine LOW/NORMAL/HIGH
determine CRITICAL
select reference range
invent thresholds
diagnose
infer medical cause
recommend treatment
override deterministic medical output
bypass OCR review
bypass Guardrail
```

Medical pipeline remains:

```text
ReferenceRangeChecker
→ CriticalDetector
→ Analyzer/RAG
→ QuestionGenerator
→ Guardrail
```

Do not reorder.

Do not duplicate medical logic.

Do not modify medical thresholds/reference data.

---

# 5. READ REPOSITORY BEFORE EDITING

Read first:

```text
presentation/AGENT.md
```

Then inspect at minimum:

```text
src/orchestrator/
src/models/orchestrator_schemas.py

src/services/llm.py

src/agents/nodes/analyzer_node.py
src/agents/nodes/guardrail_node.py
src/agents/nodes/question_generator_node.py

src/services/analyte_catalog.py
relevant retriever/RAG services
relevant source handling

src/api/routes.py

tests/orchestrator/
tests/test_agents/
tests/test_services/
```

Also inspect TIP-003 implementation and current response construction flow.

Do not rely on function names alone.

---

# 6. PRE-IMPLEMENTATION FOCUSED SCAN

Before coding, internally determine:

```text
TIP004_FOCUSED_SCAN

CURRENT_RESPONSE_BUILD_PATH=
CURRENT_LLM_MESSAGE_PATH=
CURRENT_CANONICAL_RESULT_TYPES=
CURRENT_SOURCE_TYPES=
CURRENT_ACTION_BUILD_PATH=

EXISTING_GUARDRAIL_OUTPUT=
EXISTING_RAG_FALLBACK=
EXISTING_LLM_FAILURE_BEHAVIOR=

REUSABLE_SERVICES=
REUSABLE_SCHEMAS=

FILES_EXPECTED_TO_CHANGE=
POTENTIAL_CONFLICTS=
P0_BLOCKERS=
```

If:

```text
P0_BLOCKERS > 0
```

STOP and escalate.

---

# 7. RESPONSE COMPOSER

Implement a bounded Response Composer.

Conceptual contract:

```text
INPUT:
- intent selected by server
- bounded canonical workflow result
- approved explanation context
- safe conversational context if necessary

OUTPUT:
- message only
```

Prefer a strongly typed structured model equivalent to:

```python
class ComposedMessage(BaseModel):
    message: str
```

Do not duplicate an existing type if TIP-001 already provides an equivalent.

Do not modify frozen TIP-001 contracts unless explicitly required and approved.

---

# 8. COMPOSER INPUT MINIMIZATION

Only provide the LLM information required to write the message.

May include where necessary:

```text
analyte display name
canonical deterministic status
approved reference information already computed by deterministic service
approved explanation context
bounded trend summary from Trend Service
bounded question context
safe failure reason
```

Must not include unless absolutely required:

```text
patient_id
user_id
DB primary keys
auth tokens
ownership metadata
full name
phone
email
raw session token
raw OCR token
```

Never put protected resource into a prompt before authorization passes.

---

# 9. COMPOSER PROMPT SAFETY

The composer system/prompt must explicitly establish:

```text
You rewrite/summarize approved canonical information only.

You may not:
- recalculate medical status
- reinterpret reference ranges
- create diagnoses
- infer cause
- recommend treatment
- introduce facts absent from supplied canonical context
- invent sources
- invent actions
- change status/reason code
```

Do not place unrestricted low-level tool access inside Response Composer.

Response Composer is generation-only.

---

# 10. SERVER-CONTROLLED RESPONSE ASSEMBLY

Final response must be assembled by deterministic server code.

Conceptually:

```text
server intent
server status
server reason_code
server canonical data
server data_type
server sources
server suggested_actions
server safety_notice
        +
composer.message
        ↓
OrchestratorResponse
```

Do not deserialize an LLM-produced full OrchestratorResponse.

Forbidden pattern:

```python
result = llm.with_structured_output(OrchestratorResponse)
```

Preferred conceptual pattern:

```python
result = llm.with_structured_output(MessageOnly)
```

---

# 11. MEDICAL SAFETY VALIDATION

Implement/wire a final safety validation layer after message generation.

It must fail closed.

Validate that final message does not violate approved medical boundary.

At minimum cover:

```text
diagnosis claim
medical cause claim
treatment recommendation
contradiction/override of canonical medical result
unsupported analyte generative explanation
unsafe response leaked after Guardrail block
```

Reuse existing safety/Guardrail mechanisms where appropriate.

Do NOT implement a second independent medical reasoning engine.

Do NOT create new clinical thresholds or disease rules.

If enforcing a safety property would require new medical knowledge:

```text
STOP AND ESCALATE
```

---

# 12. GUARDRAIL INTEGRATION

Existing Guardrail remains authoritative.

If upstream Guardrail returns blocked/safe fallback state:

```text
Response Composer must not resurrect blocked content.
```

Where practical:

```text
GUARDRAIL_BLOCKED
→ deterministic bounded response
→ no unsafe generative recovery path
```

Do not bypass Guardrail with:

```text
"try another LLM answer"
```

---

# 13. SCHEMA VALIDATION

Every final Orchestrator response must validate against the frozen TIP-001 schema.

Requirements:

```text
valid discriminated payload
valid data_type
valid intent
valid status
valid reason_code
valid suggested_actions
valid sources type
no arbitrary extra fields
```

Invalid final assembly must fail closed.

Do not return partially invalid JSON.

Do not loosen schemas to make tests pass.

---

# 14. SUGGESTED ACTION VALIDATION

V1 allowlist remains exactly:

```text
OPEN_REPORT
VIEW_ABNORMAL
VIEW_HISTORY
VIEW_TREND
VIEW_DOCTOR_QUESTIONS
CONFIRM_OCR
RETRY
```

No eighth action.

Reject:

```text
arbitrary URL
javascript
patient_id
user_id
authorization override
raw route injection
arbitrary payload
unknown action type
```

Suggested actions are server-built.

LLM must not directly construct privileged action payloads.

---

# 15. ACTION POLICY CONTEXT

Actions must be appropriate to current state.

Examples:

```text
OCR_REVIEW_REQUIRED
→ CONFIRM_OCR may be allowed

TREND_INSUFFICIENT_POINTS
→ must not fabricate trend action payload

unauthorized report
→ must not expose OPEN_REPORT for protected resource

Guest
→ must not receive privileged Patient-only action that would bypass auth

unsupported analyte
→ must not generate an action implying supported medical analysis
```

Follow existing contract/policy.

Do not invent new UX behavior that belongs to TIP-005.

---

# 16. RAG POLICY

Reuse existing RAG architecture.

Do NOT rewrite retriever/analyzer architecture unnecessarily.

Approved policy:

### Supported analyte

```text
approved deterministic result
        ↓
approved RAG/context if available
        ↓
bounded explanation
```

### Unsupported analyte / status unknown

Per resolved ESC-004:

```text
status="unknown"
→ general RAG invocation = 0
→ LLM explanation invocation = 0
→ approved curated catalog explanation MAY remain
→ approved catalog sources MAY remain
```

Do not regress this behavior.

---

# 17. RAG SOURCE POLICY

Sources exposed to final response must originate from approved server metadata.

LLM-generated URLs are forbidden.

Do not parse URLs out of free-form model text.

Do not invent citations.

Do not allow composer to modify source list.

Final:

```text
sources = server controlled
```

---

# 18. RAG UNAVAILABLE

Inspect current implementation before deciding fallback.

Rule:

```text
RAG unavailable
→ use approved fallback ONLY if one actually exists
```

Do not invent a new medical fallback.

If approved curated fallback exists:

```text
use it according to existing policy
```

If no approved fallback exists:

```text
return bounded RAG_UNAVAILABLE behavior
```

Do not hide RAG failure behind fabricated explanation.

---

# 19. LLM UNAVAILABLE

Inspect current approved fallback behavior.

When composer LLM is unavailable:

```text
use deterministic/template fallback ONLY if existing approved behavior exists
```

Otherwise:

```text
LLM_UNAVAILABLE
```

No second provider.

No new model SDK.

No external dependency.

Do not silently switch models/providers.

---

# 20. UNSUPPORTED ANALYTE REGRESSION

Preserve resolved ESC-004 behavior exactly.

Required:

```text
status unknown
→ RAG count = 0
→ analyzer explanation LLM count = 0
```

TIP-004 Response Composer must also not create a new loophole.

For unsupported analyte, Composer must not generate medical explanation from general knowledge.

If curated catalog text is returned:

```text
message must stay bounded to approved content/policy
```

Do not use general LLM knowledge to expand it.

---

# 21. FAILURE RESPONSE POLICY

Preserve existing ReasonCode contract.

At minimum correctly handle applicable:

```text
ONBOARDING_REQUIRED
OCR_REVIEW_REQUIRED
MEDICAL_DIAGNOSIS_REQUEST
MEDICAL_CAUSE_REQUEST
TREATMENT_REQUEST
UNSUPPORTED_ANALYTE
UNSUPPORTED_CAPABILITY
AMBIGUOUS_CONTEXT
AUTH_EXPIRED
REPORT_NOT_FOUND_OR_UNAUTHORIZED
DB_UNAVAILABLE
LLM_UNAVAILABLE
RAG_UNAVAILABLE
TREND_INSUFFICIENT_POINTS
TREND_UNIT_INCONSISTENT
UNKNOWN_INTENT
GUARDRAIL_BLOCKED
INTERNAL_WORKFLOW_ERROR
```

Do not add reason codes.

Do not alter frozen enum.

---

# 22. AUTHORIZATION SAFETY

TIP-002/TIP-003 authorization remains unchanged.

Before protected Patient data enters composer context:

```text
ownership/auth check must already have passed.
```

Response Composer never decides authorization.

Forbidden:

```text
composer sees non-owned report then decides to hide it
```

Correct:

```text
non-owned report
→ blocked before protected bytes reach composer
```

---

# 23. ZERO-INVOCATION REQUIREMENTS

Preserve established invariants.

Tests must instrument real boundaries.

At minimum verify where relevant:

### A. Unauthorized resource

```text
REPORT_NOT_FOUND_OR_UNAUTHORIZED
→ protected resource content in composer prompt = 0
```

### B. Unsupported analyte

```text
UNSUPPORTED_ANALYTE
→ RAG invocation = 0
→ medical explanation LLM invocation = 0
```

### C. Guardrail blocked

```text
blocked unsafe content
→ no unsafe secondary generation path
```

### D. OCR pending

```text
OCR_REVIEW_REQUIRED
→ medical analysis pipeline invocation = 0
```

Do not test only response text.

Count mocked/spied boundary calls.

---

# 24. RESPONSE COMPOSER TESTS

Add focused tests covering at minimum:

## TEST-R1 — MESSAGE ONLY

Attempt model output with extra properties.

Expected:

```text
only message accepted
or validation fails closed
```

## TEST-R2 — SERVER FIELDS IMMUTABLE BY LLM

Model attempts to produce:

```text
status
critical
sources
reason_code
actions
```

Expected:

```text
cannot alter server response
```

## TEST-R3 — CANONICAL STATUS PRESERVED

Given deterministic status:

```text
HIGH
```

composer cannot turn it into:

```text
NORMAL
```

or otherwise contradict it.

## TEST-R4 — DIAGNOSIS REQUEST

Unsafe diagnosis wording:

```text
blocked/safe bounded response
```

No diagnostic claim.

## TEST-R5 — CAUSE REQUEST

No unapproved medical cause inference.

## TEST-R6 — TREATMENT REQUEST

No treatment recommendation.

---

# 25. SCHEMA VALIDATION TESTS

At minimum:

```text
valid typed payload → PASS
wrong data_type/payload → FAIL
unknown action → FAIL
arbitrary URL/action payload → FAIL
extra unrestricted data → FAIL
invalid reason_code → FAIL
```

Reuse TIP-001 tests where possible.

Do not duplicate identical tests unnecessarily.

---

# 26. SUGGESTED ACTION TESTS

Required:

```text
all allowed V1 action types accepted
unknown action rejected
patient_id injection rejected
URL injection rejected
javascript rejected
unauthorized privileged action rejected
```

Also verify final response cannot carry an invalid action even if upstream code attempts to produce one.

---

# 27. SOURCE TESTS

Verify:

```text
sources originate from approved metadata
LLM cannot invent source URL
LLM output containing URL does not automatically enter sources[]
unsupported analyte uses only approved curated sources when applicable
```

---

# 28. RAG FAILURE TESTS

Test actual current supported behavior:

```text
retriever unavailable
empty retrieval
approved curated fallback
no approved fallback
```

Expected response must map to existing policy.

Do not create new medical prose solely to make failure UX nicer.

---

# 29. LLM FAILURE TESTS

Simulate composer LLM failure.

Verify:

```text
approved deterministic fallback if existing
otherwise LLM_UNAVAILABLE
```

No model-provider switching.

No full server field loss/corruption.

Final response must remain schema-valid.

---

# 30. FILE BOUNDARIES

Preferred TIP-004 implementation location:

```text
src/orchestrator/
```

Contracts remain:

```text
src/models/orchestrator_schemas.py
```

and are frozen unless an actual blocker is discovered.

Do NOT modify unrelated medical pipeline files.

Normally forbidden files in `presentation/AGENT.md` remain forbidden.

In particular do not modify without escalation:

```text
src/agents/graph.py
src/agents/state.py
src/models/schemas.py
src/api/routes.py
src/services/reference_repository.py
src/services/history_repository.py
src/services/trend_service.py
```

ESC-004's previous authorization to modify:

```text
src/agents/nodes/analyzer_node.py
```

was limited to ESC-004 closure.

Do NOT treat it as standing permission for TIP-004.

If TIP-004 appears to require another analyzer-node modification:

```text
STOP AND ESCALATE
```

---

# 31. NO TIP-005 WORK

Do not implement:

```text
Hybrid Assistant UI
chat panel
onboarding UI
frontend action renderer
OCR navigation UI
frontend session storage
responsive Assistant experience
```

That belongs to TIP-005.

Backend response/action contract only is in scope here.

---

# 32. OBSERVABILITY

If TIP-004 touches logging:

Allowed metadata:

```text
request_id
session_id_hash
intent
workflow outcome
failure code
guardrail triggered
action validation outcome
latency
```

Forbidden logs:

```text
raw lab values
raw user chat
raw medical report
auth token
OCR token
direct identifiers
```

Do not introduce new logging unless needed.

---

# 33. TEST POLICY — TIP-004

Per current Human Owner instruction:

```text
RUN:
pytest -q
```

Do not run the full backend suite for this TIP.

Use focused tests only.

Required minimum:

```text
pytest tests/orchestrator -q
```

plus directly affected focused suites, such as:

```text
tests/test_agents/test_analyzer_node.py
relevant RAG/retriever tests
relevant guardrail tests
new TIP-004 response/composer tests
```

Choose exact commands based on changed code.

Report every command.


Use wording:

```text
FULL_BACKEND_REGRESSION=NOT RUN — Human Owner instruction for TIP-004
```

---

# 34. LINT POLICY

Run Ruff only on changed/new files.

Example:

```text
ruff check <TIP-004 changed files>
```

Expected:

```text
NEW_ERRORS=0
```

Do not fix repository-wide pre-existing Ruff debt.

Do not run `make typecheck`.

Do not install mypy.

---

# 35. ACCEPTANCE CRITERIA

## AC1 — Composer authority

Given canonical server-controlled workflow result,

when Response Composer runs,

then:

```text
LLM generates message only
```

and cannot control any other response field.

---

## AC2 — Medical authority preserved

Given deterministic medical status/range/critical result,

when final response is composed,

then:

```text
no model output may alter or contradict authoritative fields
```

Medical authority violations:

```text
0
```

---

## AC3 — Final response always typed

All successful, failure, blocked and needs-input responses produced by TIP-004 path must validate against frozen response schema.

---

## AC4 — Suggested actions safe

All final suggested actions must pass allowlist/schema/policy validation.

Invalid privileged action execution:

```text
0
```

---

## AC5 — Unsupported analyte

Given:

```text
status="unknown"
```

then:

```text
general RAG invocation = 0
LLM medical explanation invocation = 0
```

Approved curated catalog fallback may remain according to ESC-004 Option B.

---

## AC6 — RAG failure

RAG unavailable must not create fabricated medical explanation.

Use only existing approved fallback.

---

## AC7 — LLM failure

LLM failure returns existing approved deterministic fallback or `LLM_UNAVAILABLE`.

Response remains typed.

---

## AC8 — Source integrity

Final `sources` must remain server-controlled.

LLM cannot inject source URLs.

---

## AC9 — Guardrail preserved

Guardrail-blocked content cannot re-enter final output through Response Composer.

---

## AC10 — Authorization preserved

No protected unauthorized resource may enter composer/LLM context.

Cross-user leakage:

```text
0
```

---

# 36. P0 TARGETS FOR THIS TIP

Focused TIP-004 tests must show:

```text
medical authority violation = 0
invalid privileged action execution = 0
cross-user leakage = 0
OCR bypass regression = 0
```

Do not claim project-wide final P0 certification yet.

That belongs to TIP-006/TIP-007.

---

# 37. STOP CONDITIONS

STOP and issue Escalation Report if implementation requires:

```text
new intent
new role
new reason code
new SuggestedAction
Orchestrator contract change
medical threshold change
reference-range change
critical-rule change
new medical logic
medical pipeline reorder
auth-model change
DB schema change
new infrastructure
new external dependency
new persistent memory
general RAG for unsupported analyte
LLM medical explanation for status unknown
modification of forbidden files not explicitly authorized
```

Do not work around these.

---

# 38. REQUIRED ESCALATION FORMAT

```text
ESCALATION REPORT

TIP=TIP-004

ISSUE=

EVIDENCE=
file
line
symbol
behavior

WHY BLUEPRINT CANNOT BE IMPLEMENTED AS WRITTEN=

OPTIONS=

OPTION A=
impact
risk

OPTION B=
impact
risk

RECOMMENDATION=

IMPACT ON MEDICAL SAFETY=
none / indirect / direct

IMPACT ON AUTHORIZATION=
none / indirect / direct

DECISION REQUIRED=
```

Then STOP.

---

# 39. REQUIRED COMPLETION REPORT

Return:

# COMPLETION REPORT — TIP-004

## STATUS

```text
DONE / PARTIAL / BLOCKED
```

## FOCUSED SCAN

```text
CURRENT_RESPONSE_BUILD_PATH=
CURRENT_LLM_MESSAGE_PATH=
RAG_FALLBACK=
FILES_RELEVANT=
```

## FILES CHANGED

For each:

```text
PATH=
PURPOSE=
CHANGE=
```

## RESPONSE COMPOSER

```text
INPUT CONTRACT=
OUTPUT CONTRACT=
SERVER-CONTROLLED FIELDS=
LLM-CONTROLLED FIELDS=
```

## FINAL ENFORCEMENT

```text
MEDICAL_SAFETY_VALIDATION=
SCHEMA_VALIDATION=
SUGGESTED_ACTION_VALIDATION=
GUARDRAIL_INTEGRATION=
```

## RAG

```text
SUPPORTED_ANALYTE=
UNSUPPORTED_ANALYTE=
RAG_UNAVAILABLE=
SOURCES_POLICY=
```

## LLM FAILURE

```text
BEHAVIOR=
FALLBACK=
```

## ACCEPTANCE CRITERIA

For AC1–AC10:

```text
AC=
PASS / FAIL

EVIDENCE=
test/function/file
```

## ZERO-INVOCATION EVIDENCE

```text
UNSUPPORTED_RAG=
UNSUPPORTED_LLM=
OCR_PENDING_PIPELINE=
UNAUTHORIZED_RESOURCE_TO_PROMPT=
```

Use actual test-boundary counts where applicable.

## TEST COMMANDS

For every command:

```text
COMMAND=
RESULT=
PASSED=
FAILED=
ERRORED=
```

Explicitly include:

```text
FULL_BACKEND_REGRESSION=
NOT RUN — Human Owner instruction for TIP-004
```

## LINT

```text
CHANGED_FILES_ERRORS=
```

## ISSUES

```text
SEVERITY=
DESCRIPTION=
IMPACT=
RECOMMENDATION=
```

## DEVIATIONS

List every departure from repository conventions.

If none:

```text
NONE
```

Do not hide approved deviations.

## GIT DIFF SUMMARY

```text
files changed
insertions/deletions
new files
unrelated worktree items untouched
```

## FINAL GATE

```text
TIP_STATUS=

P0_ISSUES=
ARCHITECTURE_DEVIATIONS=
SAFETY_DEVIATIONS=

MEDICAL_AUTHORITY_VIOLATIONS=
INVALID_ACTION_EXECUTION=
CROSS_USER_LEAKAGE=
OCR_BYPASS_REGRESSION=

RESPONSE_SCHEMA_VALID=
YES/NO

TIP005_READY_FOR_HUMAN_APPROVAL=
YES/NO
```

---

# 40. EXECUTION ORDER

Execute exactly:

```text
1. Read presentation/AGENT.md
2. Focused scan of existing TIP-003/RAG/response paths
3. Check for P0 blocker
4. Implement Response Composer
5. Implement/wire final safety validation
6. Implement/wire schema enforcement
7. Implement/wire Suggested Action validation
8. Integrate existing RAG/fallback policy
9. Add focused safety/regression tests
10. Run focused tests only
11. Ruff changed files
12. Produce Completion Report
13. STOP
```

Do not start TIP-005.

Run full `pytest -q` after implementing. 

Do not claim final project verification.
