# ROLE
You are the BUILDER for VMEC-05 Orchestrator V1.
Read the project instruction file (AGENTS.md) before anything else.
Report which path you read it from.

Your TIP-001 code is ACCEPTED in substance and is NOT being discarded.
Your report was honest — including the lint failure and the untracked files.
Two things are missing: complete acceptance evidence, and a lint fix.

Note on the 9 DB errors: your handling was correct. The waiver was genuine —
it came from Tạ Quốc Tuấn because the database was not started. One refinement
only: label such cases `UNRESOLVED — <reason> (waived by <name>)` rather than
"temporarily passed", so that an unproven criterion never reads as a proven one.

# SCOPE — TIP-001-R1
Remediation only. You will: investigate two things, fix lint, and produce a
complete Completion Report.

You will NOT add features. You will NOT change any TIP-001 contract.
You will NOT start TIP-002.

# STEP 1 — INVESTIGATE (report only, do not fix)

## 1a. Medical reference data file
`data/reference/medical_kb_manifest.json` is reported as modified.
This is medical reference data. It must not change during BUILD.

Run and report verbatim output:
  git status --short data/reference/
  git diff data/reference/medical_kb_manifest.json
  git log --oneline -5 -- data/reference/medical_kb_manifest.json

Report:
  - the exact diff (what changed, how many lines, which keys)
  - whether the modification predates your TIP-001 work
  - whether anything you wrote or ran could have touched it

DO NOT revert it. DO NOT commit it. DO NOT add it to .gitignore.

STOP AND REPORT IMMEDIATELY, without proceeding to Step 2, if the diff touches
any of: thresholds, reference ranges, critical values, analyte definitions,
units, or any medical value. That is a P0 matter for the Human Owner.

If the diff is only metadata (timestamp, checksum, build id, path), note it and continue.

## 1b. Repo enum convention and ruff baseline
Read src/models/schemas.py and src/models/ocr_schemas.py.

Report:
  - How does existing code define string enums?
    `class X(str, Enum)`, `class X(StrEnum)`, or Literal types?
    Quote TWO real examples with file path and line numbers.
  - Run ruff on the WHOLE repository: `ruff check .`
    Report: total error count
            count in pre-existing files (not touched by TIP-001)
            count in TIP-001 files
  - Do pre-existing files violate UP042 / UP007 / UP037? Yes/No with examples.
  - Quote the ruff configuration (pyproject.toml / ruff.toml / setup.cfg) —
    which rule sets are enabled, which are ignored?

# STEP 2 — FIX LINT

Fix the 13 ruff errors in:
  src/models/orchestrator_schemas.py
  tests/orchestrator/test_contracts.py

Rules:
- Unused imports in test_contracts.py (DataPayload, DataType, ResponseStatus):
  remove them ONLY if genuinely unused. If an acceptance criterion should have
  been exercising one of them and is not, that is a MISSING TEST — write the
  test, keep the import, and report it as an ISSUE.
- For UP042 / UP007 / UP037: follow what the EXISTING repo files do.
  Consistency with the repository outranks the lint suggestion.
  If the repo uses `str, Enum` throughout, keep `str, Enum` and add a targeted
  per-line `# noqa: UP042` with a one-line reason.
  State which way you went and why.
- Do not reformat, restructure, or improve anything beyond these 13 errors.
- Do not change any type contract, field name, or validation behaviour.

Verify contracts still behave identically: pytest tests/orchestrator -q

# STEP 3 — TEST RUN AND LABELLING

Run and report actual output:
  pytest tests/orchestrator -q
  pytest -q
  ruff check .                     (full repo — not scoped)

For the 9 DB errors:
  - Name the 9 tests.
  - Confirm whether any of them import or exercise orchestrator_schemas.py
    (they should not — TIP-001 adds types only).
  - Label AC13 exactly: UNRESOLVED — DB not started (waived by Tạ Quốc Tuấn)
  - Do NOT write "passed" or "temporarily passed" for them.

Do NOT modify, skip, or xfail any existing test.

# STEP 4 — COMPLETE COMPLETION REPORT

Reproduce this structure fully. Empty rows are not acceptable.

## COMPLETION REPORT — TIP-001 (revision 1)

**STATUS:** DONE / PARTIAL / BLOCKED

**REQ-IDs COVERED** — for each, name the test(s) demonstrating it:
REQ-I01, REQ-I02, REQ-R01, REQ-R02, REQ-S01, REQ-S02, REQ-S03, REQ-S04,
REQ-P01, REQ-U01, REQ-U02, REQ-F01, REQ-A02, REQ-C03

**STEP 1 FINDINGS**
1a. medical_kb_manifest.json — diff content, origin, verdict
1b. enum convention + full-repo ruff baseline + ruff config

**FILES CHANGED**
| Path | Purpose | Change |

**ACCEPTANCE CRITERIA — all 13 rows required**
| AC | Result | Test name | Evidence |
| AC1 | | | |  ... through AC13

For AC4 and AC12, PASTE THE FULL TEST FUNCTION BODY into the report.
The Contractor needs to read the assertion, not the verdict.

AC4 reminder: the test must inspect the `data` field annotation programmatically
(model_fields / typing introspection) and assert there is no dict / Any /
extra="allow" escape hatch. Parsing a valid payload does NOT satisfy AC4.
A docstring or comment does NOT satisfy AC4.

AC12 reminder: the test must assert the CONFIRM_OCR variant has no field capable
of holding an indicator value, unit, or draft row.

**TEST COMMANDS**
| Command | Result | Passed | Failed | Errored |

**ISSUES DISCOVERED**
| Severity | Description | Impact | Recommendation |

**DEVIATIONS FROM SPEC:** NONE / [what — why — impact]
(Revision 0 ran scoped ruff instead of `ruff check .` — list this.)

**GIT DIFF SUMMARY**
Files added / modified / deleted, LOC per file.
Explicitly confirm NONE of these were modified:
  src/models/schemas.py, src/models/ocr_schemas.py, src/agents/state.py,
  src/models/db.py, src/services/auth.py, data/reference/*

**FINAL GATE**
TIP_STATUS=
P0_ISSUES=
ARCHITECTURE_DEVIATIONS=
SAFETY_DEVIATIONS=
AC13_STATUS=            PASS / UNRESOLVED-DB-NOT-STARTED / FAIL
READY_FOR_NEXT_TIP=     YES / NO

Then STOP. Do not begin TIP-002.